amenity=school の Relationデータ設定パターンが以下であることを確認した。

1.線の組み合わせによるポリゴン表現（open）は、すべて外周表現（outer）のみ：312件
2.線の組み合わせによるポリゴン表現（open）と、中抜け表現（inner）の組み合わせ（中抜けポリゴン）はない
3.線の組み合わせによるポリゴン表現（open）の各線要素の並びはランダム
4.線の組み合わせによるポリゴン表現（open）の各線要素は、開始と開始が結びつくケースがある（必ずしも線の終了→次の線の開始ではない）
5.線の組み合わせによるポリゴン表現（open）と、閉領域ポリゴンの組み合わせ（マルチポリゴン）はある：1件のみ、Element:2084395/廿日市市立七尾中学校）

以上